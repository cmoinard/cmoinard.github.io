title: Christophe Moinard's Blog
author:
  name: Christophe Moinard
  email: christophe.moinard@netc.fr
description: > # this means to ignore newlines until "show_excerpts:"
  F#, Meshuggah and crème de marrons addict
keywords: jekyll, jekyll-theme, fsharp  # add your own keywords or leave empty

lang: en # the language of your site (for example: en, fr, cn, ru, etc.)
icon: 😏 # the emoji used as the favicon (alternatively, provide image name in /assets/img/)

url: https://cmoinard.github.io # the base hostname & protocol for your site
baseurl: # the subpath of your site, e.g. /blog/

show_excerpts: false # set to true to show excerpts on the homepage

# Minima date format
# refer to https://shopify.github.io/liquid/filters/date/ if you want to customize this
minima:
  date_format: "%Y-%m-%d"

  # generate social links in footer
  social_links:
    - { platform: github,         user_url: "https://github.com/cmoinard" }
    - { platform: twitter,        user_url: "https://twitter.com/chr_moinard" }
    - { platform: mastodon,       user_url: "https://piaille.fr/@cmoinard" }

# If you want to link only specific pages in your header, uncomment
# this and add the path to the pages in order as they should show up
#header_pages:
# - about.md

# Build settings
theme: minima

plugins:
 - jekyll-feed
 - jekyll-seo-tag
